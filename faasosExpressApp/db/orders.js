var express = require('express');
var router = express.Router();
var  config = require('./faasos');

/* GET users listing. */
router.get('/getOrders',function(req, res) {
    
  });

module.exports = router;
